global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/prometheus-rules.yml

scrape_configs:
  - job_name: vidyaverse-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000
